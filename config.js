// Minimal config you can edit without touching code
window.APP_CONFIG = {
  APP_NAME: "Ledger & Lens",
  PREMIUM_ENABLED: true,
  // Any string in this array will unlock "premium" features (encryption, export, bulk import).
  // Replace these with your own keys if you want to charge for access.
  LICENSE_KEYS: ["OPEN-ALL", "DEMO-123", "FAMILY-PLAN"],
  STRIPE_CHECKOUT_URL: "", // optional: paste a Checkout link here when you’re ready to charge
};